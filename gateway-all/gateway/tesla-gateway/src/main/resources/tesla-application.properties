app.id=${app.id}
app.name=${app.name}
spring.application.name=${spring.application.name}
spring.redis=${spring.redis}
spring.redis.cat.enabled=${spring.redis.cat.enabled}
server.port=${server.port}
server.type=${server.type}
dubbo.application.qos.enable=false
dubbo.protocol.id=${dubbo.protocol.id}
dubbo.protocol.name=${dubbo.protocol.name}
dubbo.protocol.port=${dubbo.protocol.port}
dubbo.registry.address=${dubbo.registry.address}
nacos.config.data.id=${nacos.config.data.id}
dubbo.group=${dubbo.group}
ref.gwdash.dubbo=${ref.gwdash.dubbo}
rpc.swimlane.group=${rpc.swimlane.group}
department.env.flag=${department.env.flag}
max.content.length=${max.content.length}
user_service_dubbo_group=${user_service_dubbo_group}
init.group=${init.group}
mtop.group=${mtop.group}
machine.group=${machine.group}
nacos.config.addrs=${nacos.config.addrs}
cache.route.path=${cache.route.path}
plugin.path=${plugin.path}
youpin.log.group=${youpin.log.group}
log.path=${log.path}

api.info.list.size=${api.info.list.size}
redis.cluster=${redis.cluster}
redis.cluster.pwd=${redis.cluster.pwd}
env.group=${env.group}
recording.traffic.topic=${recording.traffic.topic}
recording.dubbo.group=${recording.dubbo.group}

# db config
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.default.initialPoolSize=10
spring.datasource.default.maxPoolSize=20
spring.datasource.default.minialPoolSize=10

datasource.isinit=${datasource.isinit}

es.isinit=${es.isinit}
es.address=${es.address}
es.user=${es.user}
es.pwd=${es.pwd}

mongodb.client=${mongodb.client}
mongodb.database=${mongodb.database}

log.exporter.isAsycn=${log.exporter.isAsycn}
log.exporter.logInterval=${log.exporter.logInterval}
log.exporter.logDeleteAge=${log.exporter.logDeleteAge}

init.app.name=${init.app.name}

side.car.enable=${side.car.enable}

#side.car.support=auth,dubbo,http

manager.dubbo.group=tesla_ops_service
tenant.id=${tenant.id}
