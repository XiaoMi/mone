import {
  LowSync
} from "./chunk-C3WQKY7Z.js";
import {
  __privateAdd,
  __privateGet,
  __privateSet
} from "./chunk-DWA4UIM3.js";

// node_modules/.pnpm/lowdb@6.1.1/node_modules/lowdb/lib/adapters/browser/WebStorage.js
var _key, _storage;
var WebStorage = class {
  constructor(key, storage) {
    __privateAdd(this, _key);
    __privateAdd(this, _storage);
    __privateSet(this, _key, key);
    __privateSet(this, _storage, storage);
  }
  read() {
    const value = __privateGet(this, _storage).getItem(__privateGet(this, _key));
    if (value === null) {
      return null;
    }
    return JSON.parse(value);
  }
  write(obj) {
    __privateGet(this, _storage).setItem(__privateGet(this, _key), JSON.stringify(obj));
  }
};
_key = new WeakMap();
_storage = new WeakMap();

// node_modules/.pnpm/lowdb@6.1.1/node_modules/lowdb/lib/adapters/browser/LocalStorage.js
var LocalStorage = class extends WebStorage {
  constructor(key) {
    super(key, localStorage);
  }
};

// node_modules/.pnpm/lowdb@6.1.1/node_modules/lowdb/lib/adapters/browser/SessionStorage.js
var SessionStorage = class extends WebStorage {
  constructor(key) {
    super(key, sessionStorage);
  }
};

// node_modules/.pnpm/lowdb@6.1.1/node_modules/lowdb/lib/presets/browser.js
function LocalStoragePreset(key, defaultData) {
  const adapter = new LocalStorage(key);
  const db = new LowSync(adapter, defaultData);
  db.read();
  return db;
}
function SessionStoragePreset(key, defaultData) {
  const adapter = new SessionStorage(key);
  const db = new LowSync(adapter, defaultData);
  db.read();
  return db;
}
export {
  LocalStorage,
  LocalStoragePreset,
  SessionStorage,
  SessionStoragePreset
};
//# sourceMappingURL=lowdb_browser.js.map
