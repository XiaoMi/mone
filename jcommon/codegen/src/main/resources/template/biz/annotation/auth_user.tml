package ${package}.annotation;

import java.lang.annotation.*;

/**
 * 用户权限验证注解
 * 用于Controller方法参数中，自动注入当前登录用户信息
 * 
 * @author ${author}
 * @date ${date}
 */
@Target(ElementType.PARAMETER)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface AuthUser {
}

