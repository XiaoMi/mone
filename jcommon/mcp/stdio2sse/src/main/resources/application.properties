# Server configuration
server.port=8000

# SSE configuration
base.url=${base.url:http://localhost:8000}
sse.path=${sse.path:/sse}
message.path=${message.path:/message}

# CORS configuration
cors.enabled=${cors.enabled:false}