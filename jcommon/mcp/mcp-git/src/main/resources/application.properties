# MCP Transport Configuration
mcp.transport.type=grpc
mcp.grpc.port=9189

# MCP Agent Configuration
mcp.agent.name=mione_git
mcp.agent.group=staging
mcp.agent.version=1.0

# Hive Manager Configuration
hive.manager.base-url=http://xxx
hive.manager.reg.switch=true
hive.manager.token=xxxx

# Git Default Configuration
git.default.username=xxx
git.default.token=xxx

# Git Email Suffix Configuration
# 用于拼接 git commit 的 author email，格式: gitName + git.email.suffix
# 例如: 如果 gitName=zhangsan，git.email.suffix=@xiaomi.com，则最终 email 为 zhangsan@xiaomi.com
git.email.suffix=@example.com

# MCP LLM Configuration
mcp.llm=qwen
